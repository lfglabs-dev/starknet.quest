import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#6affaf",
      main: "#6affaf",
      dark: "#6affaf",
    },
    secondary: {
      light: "#6affaf",
      main: "#6affaf",
      dark: "#6affaf",
    },
  },
});

export default theme;
