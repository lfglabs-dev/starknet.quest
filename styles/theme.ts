import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      light: "#6affaf",
      main: "#6affaf",
      dark: "#6affaf",
    },
    secondary: {
      light: "#f4faff",
      main: "#f4faff",
      dark: "#f4faff",
    },
    background: {
      default: "#101012",
    },
  },
});

export default theme;
