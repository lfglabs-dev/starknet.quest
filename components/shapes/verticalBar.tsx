import React from "react";
import styles from "../../styles/components/shapes.module.css";

const VerticalBar = () => {
  return <div className={styles.verticalBar} />;
};

export default VerticalBar;
